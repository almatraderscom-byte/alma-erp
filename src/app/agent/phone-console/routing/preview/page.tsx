import { RoutingPreviewView } from '@/agent/components/phone/console/RoutingPreviewView'

export const metadata = { title: 'ফোন কনসোল — রাউটিং পরীক্ষা' }

export default function Page() {
  return <RoutingPreviewView />
}
