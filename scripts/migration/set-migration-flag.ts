#!/usr/bin/env npx tsx
/**
 * @deprecated Phase 4 complete — per-domain read flags removed. Postgres is always on.
 */
console.log('Migration read flags removed in Phase 4. Use Postgres lifestyle tables directly.')
console.log('No action needed.')
