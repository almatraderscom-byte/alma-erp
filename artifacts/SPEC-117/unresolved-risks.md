# SPEC-117 Unresolved Risks
Critical unresolved risks: **0**.

Notes: required approver roles are owner-authored config; an empty/invalid config is treated as ineligible (never silently permissive). This composes on top of SPEC-112 without changing it — callers needing SoD use resolveApprovalWithSod.
