# SPEC-121 — Changed files (owned zone src/agent/tool-gateway)
```
tool-gateway/contract.ts               (new) request/result contract + composer
tool-gateway/gateway.ts                (new) DEFAULT_STAGES (empty; grows per spec)
tool-gateway/index.ts                  (new) barrel
tool-gateway/tsconfig.json             (new) scoped typecheck config
tool-gateway/__tests__/contract.test.ts(new) 8 tests
artifacts/SPEC-121/*                        proof
```
No /api/agent, no prisma/schema, no ERP file touched.
