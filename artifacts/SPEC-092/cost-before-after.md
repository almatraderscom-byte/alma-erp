# SPEC-092 — Cost before/after
0 → 0 runtime. Its PURPOSE is cost control: the hard cap (≤24) bounds the tool
tokens the model ever sees, regardless of how broad the intent. No model/provider/
DB/network call (INV-01/03). PASS.
