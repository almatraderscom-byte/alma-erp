# SPEC-047 Contract — Tool-schema bundle
`toolSchemaBundle(schemas[], version?)` -> non-cacheable tool_schema bundle, tools sorted by name (deterministic); empty when none. Rollback: `git revert --no-edit <SPEC-047 commit>`.
