# SPEC-098 — Architecture scan
`normalize.ts` imports `@/agent/contracts`, `zod`. INV-01 (pure mapping, no LLM).
No prisma/network. No ERP→agent import. Ownership diff: only results +
artifacts/SPEC-098. PASS.
