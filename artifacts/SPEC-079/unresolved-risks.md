# SPEC-079 — Unresolved risks
1. The registry is built but not yet WIRED as the monolith's replacement in the
   live core loop — that switch is deliberately deferred (it requires enforce mode
   + owner sign-off, and touches the agent core outside G08's owned zones). SPEC-080
   is the gate that certifies the precondition (parity). Severity: low (by design;
   INV-09 keeps legacy authoritative until evidence passes).
Unresolved critical risks: 0.
