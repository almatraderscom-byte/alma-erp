# SPEC-044 Contract — Policy bundle
`policyBundle(policyText, version?)` -> cacheable policy bundle (kind policy). Part of the stable cacheable prefix. Rollback: `git revert --no-edit <SPEC-044 commit>`.
