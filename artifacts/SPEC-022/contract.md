# SPEC-022 Contract — Tokens
`TokenUsage`, `EMPTY_USAGE`, `TokenEstimator` seam, `heuristicTokenEstimator`
(chars/4), `estimateTokens`, `estimateUsage`. Deterministic, no model. Rollback:
`git revert --no-edit <SPEC-022 commit>`.
