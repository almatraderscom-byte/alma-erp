# SPEC-045 Contract — Workflow-state bundle
`workflowStateBundle(state, version?)` -> non-cacheable workflow_state bundle, deterministic sorted-key JSON. Rollback: `git revert --no-edit <SPEC-045 commit>`.
