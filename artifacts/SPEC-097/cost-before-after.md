# SPEC-097 — Cost before/after
0 → 0. Summarization is deterministic recursion — crucially it replaces the naive
"send the big result to a model to summarize" pattern with ZERO model tokens
(INV-01). No provider/DB/network call. PASS.
