# SPEC-073 — Cost before/after
All request-path metrics 0 → 0. Loader runs once at module load (validate + index
326 in-memory manifests); no model/provider/DB/network call (INV-01/03). Generator
is dev-time only. PASS.
