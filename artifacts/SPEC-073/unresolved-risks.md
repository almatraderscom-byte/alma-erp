# SPEC-073 — Unresolved risks
1. Generated `summary`/`title` are provisional (derived from name/mode); real
   descriptions live on the handlers. SPEC-079 can enrich when it wires the
   runtime registry. Severity: low (cosmetic; routing/classification are real).
2. `sideEffects` are seeded, not yet policy-checked — SPEC-075 adds the
   authoritative policy + consistency test. Severity: low (values are valid now).
Unresolved critical risks: 0.
