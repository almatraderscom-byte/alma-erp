# SPEC-087 — Changed files (owned zone src/agent/capabilities)
```
health.ts                              (new) state machine + availability + override store + boundary
index.ts                               (edit) barrel export
__tests__/health.test.ts               (new) 12 tests
artifacts/SPEC-087/*                        proof
```
No live prisma, no monolith/production file touched.
