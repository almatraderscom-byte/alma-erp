# SPEC-146 unresolved risks
- Target matching is exact-label equality; a real perception builder must produce stable, unique labels (or the action selector must disambiguate). This is a perception-quality concern handled in SPEC-147 compaction; the separation guarantee (no action on absent element) holds regardless. 0 critical risks.
