# SPEC-146 cost before/after
Core adds zero model/tool/USD cost (validation + action derivation are pure). Separation prevents wasted actions on non-present targets, reducing failed-action cost. Latency in-memory. Outcome rate 13/13. Zero cost regression.
