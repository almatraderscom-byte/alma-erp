# SPEC-043 Contract — Skill bundle
`skillBundle(skillId, instructions, version?)` -> cacheable skill bundle (kind skill, ordered after constitution, inside cacheable prefix). Rollback: `git revert --no-edit <SPEC-043 commit>`.
