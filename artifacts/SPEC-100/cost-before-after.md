# SPEC-100 — Cost before/after
0 → 0. The gate runs sample pipelines in memory; no model/provider/DB/network call
(INV-01/03). It CERTIFIES the firewall's cost-control bounds (shortlist ≤ 24,
minimized ≤ raw, view ≤ 4 KiB, normalized ≤ 10). PASS.
