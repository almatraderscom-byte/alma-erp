# SPEC-042 Contract — Constitution bundle
`constitutionBundle(content?, version?)` -> cacheable constitution bundle; `DEFAULT_ALMA_CONSTITUTION`. Front of the cacheable prefix. Rollback: `git revert --no-edit <SPEC-042 commit>`.
