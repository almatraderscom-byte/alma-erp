# SPEC-066 Baseline — Semantic read-only response cache
No semantic response cache existed. New: SemanticResponseCache — cosine-similar read-only query -> cached answer, tenant-isolated, threshold-gated. Embeddings are inputs (no model call). Additive.
