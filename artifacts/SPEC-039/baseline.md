# SPEC-039 Baseline — Cost denial & degradation policy
No denial policy existed. New: resolveDenial — default DENY (fail-closed), DEGRADE opt-in only when a cheaper option fits (owner decision). Never silently downgrades. Additive.
