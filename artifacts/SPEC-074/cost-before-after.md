# SPEC-074 — Cost before/after
All request-path metrics 0 → 0. Validation is synchronous Ajv (compiled
validators cached by schema id); bounded view is a synchronous pass. No model/
provider/DB/network call (INV-01/03). Generator is dev-time only. PASS.
