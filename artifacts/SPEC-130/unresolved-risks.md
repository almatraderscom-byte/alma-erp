# SPEC-130 — Unresolved risks
1. As production wires the gateway, Rule B begins to bind real call sites; a genuine
   adapter that must call the network uses the `gateway-adapter-ok` reviewed opt-out.
   The gate is forward-looking (like G11's), not a retroactive sweep of legacy code.
   Severity: low. Critical: 0.
