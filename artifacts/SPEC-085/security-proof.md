# SPEC-085 — Security proof
No silent model upgrade: TIER_HINTS ceilings are strictly increasing and a
capability's tier is bounded by its declared tools; a mismatch is a hard issue
(TIER_MISMATCH). `queryCostTier` enforces identity and never throws. Secret scan:
none. PASS.
