# SPEC-096 — Cost before/after
0 → 0 runtime. This bounds the RESULT tokens the model pays: every view is ≤ the
byte cap regardless of raw result size. No model/provider/DB/network call
(INV-01/03). PASS.
