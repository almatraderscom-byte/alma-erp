# SPEC-017 Contract — Planning need
`PLANNING_NEEDS`, `classifyPlanningNeed(n, complexity?)` → {planningNeed, reasons},
`planningStage`. Deterministic. Rollback: `git revert --no-edit <SPEC-017 commit>`.
