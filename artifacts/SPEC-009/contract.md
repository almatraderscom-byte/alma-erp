# SPEC-009 Contract — Proof-artifact standard
`REQUIRED_PROOF_ARTIFACTS` (10), `VERDICTS`, `verdictOf`, `validateProof`,
`isAdvanceable` (complete AND PASS). Gate `check-proof.mjs [--require-pass]`.
Zero model calls. Rollback: `git revert --no-edit <SPEC-009 commit>`.
