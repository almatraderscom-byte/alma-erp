# SPEC-102 Contract — Agent principal
`AgentPrincipal {kind:agent, tenantId, agentId, roles}`, `agentPrincipal(identity, roles?)` (agentId or actorId), schema. Fail-closed. Rollback: `git revert --no-edit <SPEC-102 commit>`.
