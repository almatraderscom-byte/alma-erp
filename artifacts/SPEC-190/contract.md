# SPEC-190 Contract — Quality and security release gate
evaluateRelease: the single ship/no-ship gate — release ALLOWED only if routing accuracy + no critical under-route, tool precision/recall, and cost-per-success all meet thresholds AND the prompt-injection + policy-bypass security suites are re-run clean; any miss ⇒ DENIED with the exact failing checks (fail-closed, INV-05). Security is re-executed here, never trusted from a cache.
Fail-closed (INV-05): when the decision cannot be verified, the safe side is chosen (ASK / DENY / not-usable), never act. Deterministic, no LLM/DB/clock — any time input is injected (INV-01). Result uses the G01 ComponentResult idiom; no boolean success, no thrown errors across the boundary. Rollback: `git revert --no-edit <SPEC-190 commit>` → parent tree MATCH.
