# SPEC-103 Contract — Workflow principal
`WorkflowPrincipal {kind:workflow, tenantId, workflowId, roles}`, `workflowPrincipal(identity, roles?)`, schema, fail-closed. Rollback: `git revert --no-edit <SPEC-103 commit>`.
