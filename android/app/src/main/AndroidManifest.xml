<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 11+ package visibility: needed for SpeechRecognizer.isRecognitionAvailable
         / createSpeechRecognizer to resolve the on-device recognition service (voice STT). -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:appCategory="productivity"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- almaerp:// custom-scheme deep links (notifications / shortcuts / future
                 widgets). Capacitor's App plugin delivers these as appUrlOpen →
                 DeepLinkManager routes them in-app (iOS registers the same scheme). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="almaerp" />
            </intent-filter>

        </activity>

        <!-- Office Live Intercom mic foreground service (Agora RTC). -->
        <service
            android:name=".IntercomForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Fires background reminders scheduled by ReminderRefresh (WorkManager). -->
        <receiver
            android:name=".ReminderAlarmReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Stage 1: WhatsApp-style full-screen incoming call. Shows over the lock
             screen, launched by CallNotifications' full-screen intent. -->
        <activity
            android:name=".IncomingCallActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/AppTheme.NoActionBarLaunch" />

        <!-- OneSignal owns the FCM service; this extension turns an office-call push
             into the full-screen call instead of a default notification. -->
        <meta-data
            android:name="com.onesignal.NotificationServiceExtension"
            android:value="com.almatraders.erp.CallNotificationExtension" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- WebRTC (office intercom / live calls in the WebView) needs this for echo
         cancellation + speaker/earpiece routing; without it call audio is raw and
         echoey on many Androids. Install-time permission, no runtime prompt. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Agora RTC headset routing for the native Office Live Intercom. -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Stage 1 calls: full-screen incoming-call notification + wake the screen. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Exact reminder alarms (BackgroundRefresh parity). USE_EXACT_ALARM is granted at
         install for reminder/alarm apps; falls back to inexact if unavailable. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Capacitor Haptics (agent/UI haptics from the WebView) drives the Vibrator. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Office Live Intercom (Agora) keeps the mic alive while backgrounded during a
         call — Android 14+ requires a microphone-typed foreground service for that. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>
