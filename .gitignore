.env.local
.env
.env.regression.local
.regression-cookie
node_modules/
.next/
out/
tmp/
*.log
*.tsbuildinfo
node
alma-lifestyle-erp@1.0.0
.env.telegram
.env*.local
.env.vercel.*
# Demo-instance database credentials — `.env` above matches only the exact name.
.env.demo
.next.stale*/
mobile/dist/*.apk
mobile/alma-erp-release.keystore
*.keystore
android/.gradle/
android/local.properties
android/app/build/
android/build/
android/app/google-services.json
android/capacitor-cordova-android-plugins/build/
.vercel

# built by scripts/build-companion-dist.mjs on every deploy
/public/companion/
/public/companion-version.json
/public/companion-updater.sh
android/local.properties

# Replay-case draft skeletons exported from real conversations (may carry PII
# until reviewed) — only reviewed, anonymized cases go in src/agent/replay/fixtures/
src/agent/replay/fixtures-drafts/

# Background sub-agent git worktrees (transient; auto-cleaned by the harness)
.claude/worktrees/

# Saved browser logins (per-site Chromium profiles) — data, never source.
worker/data/browser-profiles/

# Mac app build output (built locally with mac-app/build.sh)
mac-app/build/
