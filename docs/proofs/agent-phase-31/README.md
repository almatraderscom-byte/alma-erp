# Phase 31 proof — replay baseline diagnostic page

- **Date:** 2026-07-17 06:14 (Asia/Dhaka)
- **Where:** owner's Chrome (Chrome MCP) at `http://localhost:8317/replay-baseline.html` — the committed report `replay-baseline.html`, generated by `npx tsx src/agent/replay/run-agent-replay.ts`. Vercel deploys are intentionally DISABLED on this branch (owner instruction; `vercel.json > git.deploymentEnabled`), so the proof is the local render of the committed diagnostic page, exercised live in Chrome and re-rendered to PNG via headless Chrome for the repo.
- **Test steps:** run the CLI → serve `docs/proofs/agent-phase-31/` → open the report in Chrome → verify header metrics → scroll to the named scenarios.
- **Expected:** 150 cases; deterministic metrics; named scenarios visible with honest FAIL verdicts (baseline findings, not hidden).
- **Actual:** 150 cases · 110 pass / 40 baseline findings · task binding 70.1% · fast-path 100% · routine intent 100% · pack recall 84.8% / precision 100% · resume brief 100% · auto-continue 100% · 23 repeated-effect risks.

## Named scenarios shown (roadmap exit gate: ≥3, incl. "2–3 replies later" + "three days later")

| Scenario | Case | Verdict on current code |
|---|---|---|
| "2–3 replies later" | `rc-0144-cont-3replies-post-ta-koi` ("post ta koi?") | FAIL binding — binds to a NEW task instead of the active post run |
| "three days later" | `rc-0117-cont-gap3d-checkpoint` ("abar try koro") | FAIL binding — checkpoint not deterministically bound; resume brief ✅ |
| "90 days later" | `rc-0121-cont-gap90d-checkpoint`, `rc-0123-cont-gap90d-what-were-we-doing` | FAIL binding — state query unbound; resume brief ✅ |
| Natural continuation | `rc-0125-cont-desired-jekhane-chile` ("যেখানে ছিলে সেখান থেকে করো") | FAIL continuationText + binding |

## Files

- `replay-baseline.html` / `replay-baseline.json` — the committed diagnostic report (deterministic; regenerate with the CLI).
- `proof-01-metrics-and-categories.png` — header metrics + per-category table + first case rows.
- `proof-02-named-scenarios-full.png` — full page including every named-scenario row.
