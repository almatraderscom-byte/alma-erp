# iOS Native-Frame Program — Handoff (START HERE)

**Owner:** Maruf (non-engineer). **Reply in Bangla, concise, plain language.**
**Goal:** make the ALMA ERP iOS app *feel like the Claude iOS app* — a fully native iOS feel.
**Approach (owner-approved, locked):** "Option 1 / native frame" — native chrome (tab bar, headers, nav, gestures, haptics, floating buttons in Swift) wrapping the existing web ERP as content ("embed mode"). We do NOT rebuild every screen.

> **Resume phrase:** the owner pastes *"docs/ios-native-frame-handoff.md poড়e ekhan theke continue koro"*. This file must let you continue with zero re-discovery.

---

## 0-c. ✅ 2026-07-05 (later) — PURE GLASS HEADER, BUILD 34

Owner spec: every page's top header = pure glassmorphism — transparent bg + STRONG backdrop blur, ZERO black shadows / dark overlays / dark gradients; content scrolling under must read blurred + glossy in the app's own colours. Implementation (all in `SpikeNativeShell.swift`):

- **`AlmaGlassHeaderView`** — a `UIVisualEffectView(UIBlurEffect(.regular))` whose material TINT sublayers are stripped (hide every non-`Backdrop`, non-contentView subview; class-name introspection only, no private API; degrades to a stock blur if iOS renames internals). This is the trick the earlier "every UIKit blur carries a tint" conclusion missed: the tint and the blur live in SEPARATE sublayers — drop the tint, keep the gaussian. Sim-verified: `.systemUltraThinMaterial` on the bar STILL dark-banded the purple aurora in dark mode; the stripped view lets the coral chat bubble / purple aurora glow through the bar blurred, zero tint, BOTH modes.
- **`AlmaTheme.installGlassHeader(nav)`** (called from `applyNav`, tag-guarded idempotent) pins one strip per nav controller: `nav.view` top → `navigationBar.bottomAnchor`, inserted BELOW the navigationBar (title/buttons stay crisp above), non-interactive. One hook covers all 5 tabs + every pushed screen incl. the native More table (which a web-side strip never could). Native strip = no WKWebView compositor re-activation pop-in (the reason the OLD web frost strips were removed).
- Bar appearance stays `configureWithTransparentBackground()` (paints nothing itself); `glassBarButton` black drop-shadow and `coralBarButton` coral shadow removed — zero `layer.shadow*` in the header zone.
- **Build 34** = build 33 + this. Sim-verified light + dark on Dashboard / Orders / Assistant / More before upload.

## 0-b. ✅ NIGHT SESSION 2026-07-05 — owner's 9 live-feedback issues fixed, BUILD 33 SHIPPED

Owner tested build 32 on-device and filed 9 issues; all fixed same night, **build 33 uploaded ("Upload succeeded" ~21:29)**. Key outcomes: (1) **bars = ZERO paint** — every UIKit material tints, so nav+tab bars are fully transparent (no material, no layer shadow, no web frost strips — the strips popped in ~1s after tab switches via compositor re-activation and were removed outright; owner rule: NOTHING may ever paint in the bar zones); (2) full-bleed WebViews under both bars everywhere + `--alma-top-inset`/`--alma-bottom-inset` injected by every tab (Dashboard's top measured from the nav bar's REAL bottom — Capacitor pads safeAreaInsets); (3) theme is deterministic — UserDefaults is the ONLY launch source (cookie read-back reverted launches; cookie is now one-way native→web) + tabs re-assert theme/insets on viewWillAppear (iOS suspends bg WebViews and drops theme-flip JS); (4) agent composer gap fixed (web-nav 3.5rem reserve + doubled env(safe-area-inset-bottom) — both native-gated in agent-ambient.css); (5) voice: WKUIDelegate mic auto-grant for our origin + mediaTypesRequiringUserActionForPlayback=[] (plain WebViews re-prompted getUserMedia per call → orb died after 2-3 taps); (6) **টুডু pill**: body-portal + opaque bg, pinned into the native title row via --alma-top-inset (globals.css `owner-todo-fab`/`owner-todo-panel`); (7) approvals list dropped its all-breakpoints min-w-[720px] (phone horizontal scroll gone) + ONE spinner per approve + `.alma-toast` redesigned as theme glass w/ tone accent bar; (8) Orders mobile: status chips = single edge-to-edge scrollable row.

**Voice truth (deep audit):** "ALMA" wake word NEVER worked on iOS — `useWakeWord` uses webkitSpeechRecognition, absent in ALL iOS WebViews incl. the old Capacitor app (it worked on desktop Chrome only). NativeSpeechBridge is a hard Capacitor plugin (CAPPlugin, opt-in `alma_native_stt` default OFF) — NOT attachable to the plain tabs without a web+Swift port (report in session log). Real iOS wake word = new SFSpeechRecognizer always-listening bridge — offered to owner as a future feature. The web voice page still shows an "ALMA ডাক চালু" chip on iOS — misleading, candidate cleanup.

**Sim gotchas added this session:** UISwitch needs a small left_click_drag (ignores synthetic clicks); sim WebViews cling to stale bundles even across relaunches — tap the per-tab "নতুন আপডেট" banner; the owner uses the Mac mid-session (window moves, Universal Control steals focus — re-screenshot before every batch).

## 0. ✅ RESOLVED 2026-07-05 (late session) — frosted bar root-caused, redesigned with owner sign-off, build 32 shipped

The frosted-bar saga is CLOSED. Root cause of the "grey slab" (why 3 rebuilds never matched Claude): **the web's full-width "🏢 Agent অফিস লাইভ" day-shift strip** (`.agent-top-strip`, pale `bg-emerald-50/90`) was the topmost element carrying the top inset, so its pale background ran from y=0 up under the translucent nav bar — the bar was blurring a pale strip, not the chat, and read as a flat grey slab in BOTH modes. A second confusion: during verification the app sat in DARK mode without realising (see §2.9), so "light frosted" was never actually seen.

**Owner-confirmed design (AskUserQuestion, all 3 = recommended):** (1) agent chat surface FOLLOWS the theme — light cream chat in light mode (the web's token system already did this; no web theme work was needed), (2) the live banner becomes a **compact floating glass pill** under the bar (web `main` commit `f61741c3`, gated `alma-native-hdr`; long tail hidden via `.agent-strip-detail`), (3) buttons hubohu Claude — light frosted circle + dark ☰ / solid coral compose / semibold title (native commit `9fef6021`: semibold title, hairline rings, calmer coral shadow).

**Verified in sim BOTH modes (2026-07-05 ~16:00): light = cream chat + light frosted bar + dark title + pill + bubble blurring under the bar; dark = same in dark. Orders/More/Dashboard also verified in both modes. Build 32 uploaded, "Upload succeeded".** Owner still needs to FEEL it on device — if he wants micro-tweaks (blur strength, pill size), they are small one-liners now.

---

## 1. CURRENT STATE — 2026-07-05 (read carefully)

- **Native branch (all iOS shell work): `claude/ios-s0-native-shell-spike`.** Native file = `ios/App/App/SpikeNativeShell.swift`. Worktree `exciting-chatelet-616c32` is GONE — 2026-07-05 late session rebuilt the env in `.claude/worktrees/tender-haibt-1b2d3c` (fresh worktree needs the §2.1 setup again).
- **⚠️ PARALLEL WORK on the same branch:** another session shipped **build 31 = "P3 mobile companion"** (`AlmaCompanionViewController`, agent drives a browser on the phone). **Expect more parallel commits — always `git fetch` + rebase before pushing.**
- **Web is on `main`, LIVE on prod** (`alma-erp-six.vercel.app`). The native app loads live prod in WebViews, so a web merge to main changes the app after the WebView's **service worker updates** (Rule §2.2).
- **TestFlight builds:** **32 = the current one** (uploaded 2026-07-05 late session: Claude-exact agent bar + banner-pill pairing + the FULL light/dark theme, both modes sim-verified; "Upload succeeded", VALID-poll not run — API key lost, see §4). 30 was never uploaded (superseded). 31 = P3 companion.
- **Theme (`5b2034f8` + polish `9fef6021`) is now sim-verified in BOTH modes and SHIPPED in 32** — More-tab Dark Mode switch flips tab bar + nav bars + all WebViews live (Orders dark verified without reload). Owner sign-off on real device still pending.

### ✅ DONE + owner/sim-verified this session (the 5 "next tasks" from the prior handoff)
1. **Glass under-scroll (content passes UNDER the agent bar):** works via a **native-injected inset** — `env(safe-area-inset-top)` is unreliable in the plain WKWebView (`contentInsetAdjustmentBehavior=.never` zeroes it), so the shell injects the exact bar height into `--alma-top-inset` (like `--kb-inset`) and the web uses `padding-top: calc(1rem + var(--alma-top-inset, env(safe-area-inset-top)))` on `.agent-thread-content` (+ `.agent-main-col{padding-top:0}`), gated `alma-native-hdr`. The agent WebView is pinned to `root.topAnchor`. **Verified: a chat line clips + frosts UNDER the bar when scrolling.** ← the MECHANISM is right; the owner's complaint is the bar's LOOK, not the under-scroll.
2. **Dashboard top overlap fixed** (web): `html.alma-native-hdr .page-header{padding-top:calc(0.5rem + env(safe-area-inset-top))}`. Verified in sim.
3. **Premium loader** (native `AlmaPremiumLoader`: breathing violet orb + morphing dots, theme backdrop) replaced the plain spinner + killed the white flash. Code-verified (the sub-second flash is uncatchable in the fast sim).
4. **Full RADIAL AssistiveTouch menu** — rewrote `AgentAssistiveNav` to a fanned arc of frosted icon discs around the FAB (Chat/Studio/WhatsApp/Monitor/Costs), staggered spring. **Verified in sim — looks great.**
5. **Glassy drawer back** — order-detail drawer close moved to a top-LEFT frosted back chevron (was hidden behind the Orders page's root-level floating chips). Verified in sim.

All web changes for the above are **live on `main`** (gated `alma-native-hdr`, no-op for web/desktop; commit chain `c7095150 → 711ace56 → 1ec53dc`).

### 🟡 IN PROGRESS / NOT approved
- **Frosted top bar redesign** (see §0) — the big open item.
- **Full app light/dark theme (WIP commit `5b2034f8`, builds clean, LIGHT mode verified, DARK mode NOT yet visually verified, NOT owner-approved):** a central `AlmaTheme` manager (mirrors the web `alma-theme` cookie; UserDefaults for a no-flash launch read) restyles the WHOLE shell for light/dark: tab bar + every nav bar (clean Claude frosted, no violet slab) + roots + loaders + the agent's hamburger/coral buttons; ERP web tabs push `data-theme` into their WebView on `.almaThemeChanged`; the Capacitor Dashboard's WebView is flipped from the tab-bar controller. **More tab has a new "Appearance → Dark Mode" switch** (`AlmaTheme.set/toggle`). The More menu now uses **system colours** so it adapts. Owner asked for this ("app-e 2 theme; sim-e shudhu light; mode change korar option nei; 2 mode-ei sob same") and chose **whole-app scope + toggle in More**. **Next session must verify dark mode looks right in the sim (I got interrupted before toggling it) and get owner sign-off on both modes.**

---

## 2. RULES / LESSONS (internalise — these cost real time last session)

1. **⚠️ FRESH-WORKTREE iOS BUILD needs 3 setup steps** (a new git worktree has none of them): `npm ci` (full — the native branch's Podfile needs `@aparajita/capacitor-biometric-auth`, `@capacitor/local-notifications`, `@capawesome/capacitor-app-shortcuts`, which the main worktree's node_modules lacks, so symlinking fails) → `export LANG=en_US.UTF-8 && cd ios/App && pod install` → **`npx cap copy ios` FROM THE WORKTREE ROOT** (generates `ios/App/App/{public,config.xml,capacitor.config.json}`; run from `ios/App` it errors "ios platform has not been added") → then `xcodebuild`. Also: `xcodebuild … | tail -N` masks the real exit code (you get `tail`'s 0) — capture to a log + `grep 'BUILD SUCCEEDED|FAILED'`.
2. **⚠️ SERVICE-WORKER / bundle cache:** each web WebView (Assistant/Orders/Dashboard separately) loads the LAST-cached bundle on launch and shows a **"নতুন আপডেট পাওয়া গেছে — এখনই রিফ্রেশ করুন"** banner ~90s later (`app-update.ts` build-id poll). To verify a web change in the sim you MUST tap that refresh **per-tab** (the SW is network-first `cache:'no-store'` for navigations but the in-memory bundle persists until reload). The owner's real fresh install loads current prod, so this is a dev-loop artifact.
3. **⚠️ UNIVERSAL CONTROL blocks sim-driving:** the owner's Mac has a linked iPad; Universal Control steals focus from `computer-use` between screenshot and click (and `osascript`/System-Events Accessibility is intermittent). Workarounds: re-`open_application "Simulator"` immediately before each click and retry on the UC error; `osascript -e 'tell application "Simulator" to activate' -e 'tell application "System Events" to click at {x,y}'` (screen POINTS, ~1.75× the computer-use screenshot px) sometimes works atomically. **Ask the owner to disable Universal Control / unlink the iPad for sim-verify sessions** — he did mid-session and it helped, but it re-enables.
4. **`xcrun simctl io <udid> screenshot /tmp/x.png` + Read** is the flake-proof capture (works even when the GUI is busy). Crop with PIL for detail.
5. **The plain WKWebView `env(safe-area-inset-top)` is UNRELIABLE** under `contentInsetAdjustmentBehavior=.never` — inject the value natively (the `--alma-top-inset` pattern, like `--kb-inset`). Don't trust env() there.
6. **UIKit bar-button gotchas** (cost 3 rebuilds): a frame-set `UIVisualEffectView` inside a constraint-sized (0×0-at-init) container gets squished to a lens + drops the glyph to the bottom → use **full Auto Layout** (pin the blur to the container edges, centre the icon in `blur.contentView`). `UIButton.setImage` re-layers the image BEHIND an added blur subview → add the icon as an explicit `UIImageView` in `blur.contentView`. Bake the icon colour with `.withTintColor(color, renderingMode:.alwaysOriginal)` (tintColor washes out over frosted material).
7. **Deploy web to main via FF/rebase branch off origin/main:** `git fetch origin main; git branch -f tmp origin/main; git switch tmp; <edit or cherry-pick>; git rebase origin/main; git push origin HEAD:main`. Gated `alma-native-hdr` → zero effect on web/desktop. Poll `curl -s https://alma-erp-six.vercel.app/api/build-info` for `commitShort` to confirm live.
8. **Owner mandate:** work non-stop, decide as owner on non-safety choices, batch into ONE TestFlight build per batch (uploads have a rolling 24h limit — sim Debug builds are free), verify hard on the sim, Bangla summary at the end. Safety/physical steps (Mac password, real-device tests, login) stay his.
9. **⚠️ KNOW WHICH THEME MODE YOU'RE IN before judging any visual.** A whole verification round was wasted comparing "light frosted vs Claude" while the app silently sat in DARK mode. Related sim-driving quirk: the More-tab Dark-Mode **UISwitch ignores synthetic computer-use CLICKS but responds to a small `left_click_drag` across the knob** (e.g. (x,y)→(x±26,y)); real finger taps are unaffected. Always zoom the switch after toggling to confirm state, and remember the sim window MOVES (owner drags it) — re-screenshot for fresh coordinates before every batch.
10. **The sim's prod session can be logged out** (it was this session — all tabs showed Sign in). The owner logs in himself (never type his credentials); after login all WebViews share it again. His Chrome-profile logins are separate from the MCP tab group's profile — `?native=1` preview may still redirect to /login even when "his Chrome is logged in".

---

## 3. ARCHITECTURE (native = `ios/App/App/SpikeNativeShell.swift`)

- **`AlmaTheme`** (NEW, top of file): single source of truth for light/dark. Reads/writes the web `alma-theme` cookie (+ UserDefaults `alma-theme-mode` for a synchronous no-flash launch read), `.almaThemeChanged` broadcast, palette (`rootBg`/`navTitle`/`tabBarBg`/`coral`/`violet`), `applyNav(_:)` (Claude frosted nav appearance for the current mode), `tabBarAppearance()`, `applyJS()` (forces web `data-theme`+cookie live), `set(dark:)`/`toggle()`/`loadInitial()`/`loadFromCookies()`.
- **`AlmaTabBarController`** (root, 5 tabs): reads the theme at launch, observes `.almaThemeChanged`, `applyTheme()` restyles tab bar + every nav bar + flips the Capacitor Dashboard's WebView (via `firstWebView(in:)` — avoids importing Capacitor). Tab 0 Dashboard = the storyboard's Capacitor `AlmaBridgeViewController` (MUST stay Capacitor — push/reminders/N1–N5). Has the P3 `ALMA_OPEN_COMPANION` self-test hook.
- **`AlmaWebTabViewController`** (Orders/Approvals/More-pushed + Assistant): shared `WKProcessPool`+`WKWebsiteDataStore` (shared login). Agent tab = `agentSegments` non-empty → pinned to `root.topAnchor` (glass under-scroll), injects `--alma-top-inset` (`setAgentTopInset` on didFinish + `viewSafeAreaInsetsDidChange`), native "ALMA AI" title + `applyAgentBar()` (hamburger `glassBarButton(light:!isDark)` + `coralBarButton`). Observes `.almaThemeChanged` → root bg + `applyJS()` + rebuild bar. `updateBackButton()` (ERP back chevron, theme-aware). `AlmaPremiumLoader` first-paint loader. Keyboard: native injects `--kb-inset` (do NOT resize the WebView — Rule).
- **`glassBarButton(icon:target:action:light:)`** = frosted circular button (light-frosted white / dark-frosted). **`coralBarButton`** = solid coral compose. **`AgentAssistiveNav`** = the radial AssistiveTouch FAB. **`AlmaPremiumLoader`** = orb+dots loader. **`AlmaCompanionViewController`** = P3 phone companion (other session).
- **`MoreMenuViewController`**: system-coloured (theme-adaptive) grouped list — section 0 = **Appearance / Dark Mode switch**, section 1 = business switcher, 2+ = module groups.
- **Web (embed mode, on `main`, gated):** `src/lib/native-shell.ts`, `src/components/layout/NativeShellBridge.tsx`, embed CSS in `src/app/globals.css` (`html.alma-native…`, `.agent-thread-content`/`.agent-main-col`/`.page-header` insets), `src/agent/components/AgentApp.tsx`/`AgentThread.tsx` (`agent-main-col`/`agent-thread-content` classes), order drawer in `src/app/orders/page.tsx`. Theme system: `src/lib/theme.ts` (cookie `alma-theme`), `src/components/providers/ThemeProvider.tsx` (`data-theme` on `<html>`), toggle in `src/components/layout/Sidebar.tsx` (NOT in the agent — that's why the native More-tab toggle was added).

---

## 4. RECIPES

- **Sim self-test:** device **iPhone 17 Pro Max** udid `94E0186B-5CDA-4708-9368-53B4FF7274E7`, bundle `com.almatraders.erp`, app passcode `Maruf@123` (memory `reference_ios_sim_access`, NOT git). Build: `export LANG=en_US.UTF-8; xcodebuild -workspace ios/App/App.xcworkspace -scheme App -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -derivedDataPath /tmp/alma-sim-dd build > /tmp/b.log 2>&1` → `simctl install/launch` → drive with computer-use/osascript → `simctl io <udid> screenshot /tmp/x.png`. The passcode field is NATIVE (computer-use `type` reaches it). The Assistant tab is testable logged-in.
- **TestFlight (cloud-signing): ⚠️ the ASC API key file is LOST** (`~/.appstoreconnect/private_keys/AuthKey_T875C2865Y.p8` no longer exists anywhere on the Mac — it lived in /tmp or the deleted worktree). **It is NOT needed:** build 32 uploaded successfully using the Apple ID session already signed into Xcode (keychain) — just OMIT the `-authenticationKey*` flags: bump `CURRENT_PROJECT_VERSION` in **4** places in `project.pbxproj`; `xcodebuild … -configuration Release -destination 'generic/platform=iOS' -archivePath /tmp/a.xcarchive -allowProvisioningUpdates archive`; then `xcodebuild -exportArchive -archivePath … -exportOptionsPlist /tmp/alma-export-opts.plist -allowProvisioningUpdates` (`method=app-store-connect, destination=upload, signingStyle=automatic, manageAppVersionAndBuildNumber=true, teamID=5D9FLR3MMA`) → look for "Upload succeeded". VALID-state API polling needs the lost key (app id `6786929629`, keyID `T875C2865Y`, issuer `4ea79058-88d0-4dbc-9010-78cf543b1790`) — if ever needed again, the owner must create a NEW API key in App Store Connect (a lost .p8 can't be re-downloaded); otherwise just ask the owner to check TestFlight processing on his phone. **Rolling 24h upload limit — one upload per batch.**

---

## 5. IMMEDIATE NEXT STEPS (in order)
1. **Owner device-tests build 32** (bar look both modes, Dark Mode switch feel, pill tap). Collect verdicts; micro-tweaks (blur strength, pill size/pos, title size) are now one-liners in `AlmaTheme`/`applyAgentBar`/the pill CSS block in `globals.css`.
2. Known small nits (not blockers): the premium loader's backdrop is created once in `loadView` so a mid-session theme flip may leave one stale first-paint backdrop (recreate on `.almaThemeChanged` if the owner notices); the More-tab Dark-Mode UISwitch ignores synthetic computer-use CLICKS (drag works, real fingers fine — sim-driving quirk only).
3. S5 then closes; S6 (SwiftUI screens) stays a separate future program (see prior handoff §4 rationale — Dashboard must stay Capacitor).
4. Coordinate with the parallel P3 session (always fetch+rebase; the branch moves under you).

## 6. GOTCHAS
- Mac reboot clears git identity → `git config user.name "Maruf Billah"` / `user.email "marufbillah@Marufs-Mac-mini.local"` before committing.
- `CURRENT_PROJECT_VERSION` in **4** places in `project.pbxproj`.
- Sim WebViews share the owner's live-prod session — avoid destructive taps (an accidental tap once opened a real order's Cancel dialog).
- Memory of record: **`full-native-ios-frame-program-option-1`** (blow-by-blow, updated with build 29/30/theme + gotchas), `reference_ios_sim_access`, `project_apple_developer_enroll`, `feedback_*`.
