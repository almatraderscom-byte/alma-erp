# Copy to .env.regression.local (gitignored) for local authenticated regression.
# NEVER commit real values.

REGRESSION_BASE_URL=https://alma-erp-six.vercel.app

# Option A: production SUPER_ADMIN session (from DevTools → Cookies)
REGRESSION_COOKIE=__Secure-next-auth.session-token=

# Option B: SUPER_ADMIN login (smoke obtains cookie at runtime)
# REGRESSION_IDENTIFIER=
# REGRESSION_PASSWORD=

DATABASE_URL=
CRON_SECRET=
REGRESSION_BUSINESS_ID=ALMA_LIFESTYLE
